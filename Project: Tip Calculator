print("Welcome to the tip calculator!")
bill = float(input("What was the total bill? $"))
tip = int(input("How much tip would you like to give? 10, 12 or 15? "))
tip = bill * (tip/100)
ppl_split_bill = int(input("How many people to split the bill? "))
final_amt = (bill + tip) / ppl_split_bill
formatted_final_amount = "{:.2f}".format(final_amt)
print(f"Each person should pay: {formatted_final_amount}")
